= render 'form'

.text-center.edit-links
  = link_to "Delete post", image_path(@image.id), method: :delete, data: { confirm: "Are you sure you want to delete this post?" }
  |
  = link_to "Cancel", images_path