- unless @images.empty?
  - @images.each do |image|
    = render 'image', image: image
- else
  .posts-wrapper
    .post
      .post-head
        %h4 You haven't followed anyone...
      .post-body
        = link_to "Find someone. Anyone", display_all_path, class: 'btn btn-warning'
        